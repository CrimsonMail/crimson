<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64">
      <Configuration>Debug</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <ProjectGuid>{F7F39E0E-A94B-4E1D-8C89-2E893919B031}</ProjectGuid>
    <RootNamespace>crimson</RootNamespace>
    <!-- 10.0 means "the latest installed Windows SDK" rather than a pinned
         version. CI pins the toolchain by pinning the runner image. -->
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <Import Project="$(MSBuildThisFileDirectory)..\..\build\Crimson.Toolset.props" />

  <PropertyGroup Label="Configuration">
    <ConfigurationType>StaticLibrary</ConfigurationType>
    <CharacterSet>Unicode</CharacterSet>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ImportGroup Label="PropertySheets">
    <Import Project="$(MSBuildThisFileDirectory)..\..\build\Crimson.Common.props" />
  </ImportGroup>

  <ItemGroup>
    <ClInclude Include="version.h" />
    <ClInclude Include="net\byte_stream.h" />
    <ClInclude Include="net\endpoint.h" />
    <ClInclude Include="net\net_error.h" />
    <ClInclude Include="net\stream_helpers.h" />
  </ItemGroup>

  <ItemGroup>
    <ClCompile Include="net\stream_helpers.cpp" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
